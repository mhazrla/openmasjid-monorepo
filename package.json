{
  "name": "openmasjid-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm dev:server\" \"pnpm dev:client\" --names \"SERVER,CLIENT\" --prefix-colors \"blue,green\"",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "build:client": "pnpm --filter client build",
    "build:server": "pnpm --filter server build",
    "build": "concurrently \"pnpm run build:client\" \"pnpm run build:server\"",
    "start:prod": "pnpm --filter server start:prod",
    "db:push": "pnpm --filter server db:push",
    "db:studio": "pnpm --filter server db:studio",
    "db:seed": "pnpm --filter server db:seed",
    "db:fresh": "pnpm --filter server db:fresh",
    "db:reset": "pnpm --filter server db:reset",
    "db:migrate:pglite": "pnpm --filter server db:migrate:pglite"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  }
}
